from setuptools import setup, find_packages

setup(
    name='cryptrader',
    version='0.1.0',
    description='A modular cryptocurrency trading library',
    author='Your Name',
    packages=find_packages(),
    install_requires=[
        'requests',
    ],
)
